type mergedObject = {
    aka?: any[];
    eightSocks?: any[];
    sf?: any[];
    city?: any[];
    adNn?: any[];
    adS?: any[];
    mir?: any[];

    identifiers: {
        personalNumber?: string;
        identityCard?: string;
        goalUser?: string;
    };

    updatedAt: Date;
};

export default mergedObject;
